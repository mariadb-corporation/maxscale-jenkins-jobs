- job:
    name: remove_lock
    description: 'This job removes vagrant_lock'
    parameters:
      - !include: './maxscale_jobs/include/slave.yaml'
    builders:
      - shell: 
          'rm $HOME/vagrant_lock'
