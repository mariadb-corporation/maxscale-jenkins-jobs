string:
  name: scm_source
  default: !include: './maxscale_jobs/include/default_perf_branch.yaml'
  description: 'Tag (refs/tags/<tagName>), branch (refs/heads/<branchName>), commit id, regex, or any other valid git source for git SCM plugin'
