conditional-step:
  condition-kind: regex-match
  regex: $pattern
  label: $git_branch
  steps:
    - trigger-builds:
        - project: $job_name
          block: true
          predefined-parameters: |
            test_set=$test_set
