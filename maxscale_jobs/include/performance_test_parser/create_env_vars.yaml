shell: |
  echo "BUILD_TIMESTAMP=$(date +%Y-%m-%d\ %H-%M-%S)" > $WORKSPACE/initial_env_vars_$BUILD_ID
  echo "SYSBENCH_RESULTS_RAW=\"Benchmark log parsing finished with an error\"" >> $WORKSPACE/initial_env_vars_$BUILD_ID
