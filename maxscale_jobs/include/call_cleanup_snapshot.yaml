trigger-parameterized-builds:
  - project: 'smart_remove_lock_snapshot'
    condition: UNSTABLE_OR_WORSE
    predefined-parameters: |
      name=$name
      build_full_name="$box-$product-$version-permanent"
    node-parameters: true
